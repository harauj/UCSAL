CREATE MEMORY TABLE PUBLIC.USUARIO(ID BIGINT GENERATED BY DEFAULT AS IDENTITY(START WITH 0) NOT NULL PRIMARY KEY,LOGIN VARCHAR(100) NOT NULL,SENHA VARCHAR(100) NOT NULL)
INSERT INTO USUARIO VALUES(0,'user','123')